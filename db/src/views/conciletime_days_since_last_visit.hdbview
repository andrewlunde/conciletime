VIEW "CONCILETIME_DAYS_SINCE_LAST_VISIT"
AS SELECT 
	"UUGUID",
	MAX("CREATEDAT") AS "LAST_VISIT",
	DAYS_BETWEEN(MAX("CREATEDAT"),NOW()) AS "DAYS_SINCE_LAST_VISIT"
FROM "CONCILETIME_UUVISITS" GROUP BY "UUGUID"